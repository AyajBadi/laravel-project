// Optional: Smooth navigation or future animations
document.querySelector('.hero-button')?.addEventListener('click', function (e) {
  e.preventDefault();
  window.location.href = 'products.html'; // Replace if needed
});
